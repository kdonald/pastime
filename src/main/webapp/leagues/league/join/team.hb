<section>
  <p>
    We see that you are captain of the <a href="{{path}}">{{name}}</a>.
    We assume you wish to join as {{name}}.
  </p>
  <h4>{{name}} Players</h4>
  <ul>
    <!-- Players added dynamically -->
  </ul>
  <a href="join?newteam">Join as a new team</a> instead of {{name}}</a>  
</section>