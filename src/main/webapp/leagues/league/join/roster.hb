<section>
  <h4>Roster</h4>
  <ul>
    <!-- Players added dynamically -->
  </ul>
  <div id="rosterSummary">{{minPlayers}} min {{maxPlayers}} max <span class="counter" data-bind="playerCount">0</span>/{{maxPlayers}}</div>
</section>